[error] [manifestModelMissing] plugin.json:0: Manifest slug "IntegralFlux" has no matching createModel() call in source -- Rack will refuse to load the ENTIRE plugin, not just this module

Historical false positive: IntegralFlux used a custom plugin::Model implementation that the analyzer did not recognize. It now uses createModel<IntegralFlux, IntegralFluxWidget>("IntegralFlux") in src/IntegralFluxWidget.cpp, with the concrete module declared in src/IntegralFlux.hpp and implemented in src/IntegralFlux.cpp. The UI compiles independently; no implementation include or custom model factory is needed. Registration in plugin.cpp and the saved module slug are unchanged.
