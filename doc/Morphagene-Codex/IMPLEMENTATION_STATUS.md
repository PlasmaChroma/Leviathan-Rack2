# Chimera implementation status

Updated 2026-09-24. Checkout: branch `expander`, commit `ee5f8ff21d457b73b3752b303dd79bd22da56010` at the initial baseline. Existing dirty and untracked work was preserved. See `INTEGRATION_BASELINE.md` for source-backed dependency and lifecycle findings.

## Gates

| Phase | State | Evidence / next work |
|---|---|---|
| 0 — integration baseline | **Complete** | Native `plugin.dll` builds; Rack-independent and Rack-linked probes pass. A headless Rack patch-manager fixture archives and reopens coherent assets for Save and alternate-path Save, exercises autosave, asset failure, bypass/stopped save, shutdown, and API-equivalent duplicate paths. Frozen IDs, latency, event-history and memory bounds, and lifecycle map are recorded. Full `test-fast` has an unrelated Sibyl fixture failure. |
| 1 — types/math and deterministic core harness | **Complete (isolated scope)** | Rack-independent C++11 schema, profile-1 transfers, reader/window primitives, 64-bit core frame count, double source coordinate, explicit input/events/output harness, allocation trap and frozen-vector runner pass. Isolated CTL-003..011/013 and DSP-001/002/005/006/011/013 are covered; registration was added later in Phase 3. |
| 2 — Reel ownership and service foundation | **Complete (synthetic foundation)** | Full-capacity paged Reel, COW snapshots, ownership token, bounded queues, two-worker service, and 256 MiB ledger pass focused native/sanitizer tests. The registered module now prepares memory lazily, adopts/retires stores by acknowledgment, and issues/reclaims immutable snapshot leases through the audio or stopped-host maintenance owner. The Rack-linked fixture tests frozen reads during overwrite, multiple readers, and two stopped-host cuts. Real patch asset writing remains Phase 7. |
| 3 — first audible slice | **Vertical slice complete; formal gate partial** | Exactly one model and manifest entry load in Rack. The temporary 28 HP panel exposes S.O.S., Vari-Speed, Slide, REC, stereo I/O, PLAY/CLOCK/REC gates, CV OUT and EOSG. The 48 kHz slice supports full-Splice signed rate/Slide, Play retrigger, Current/Append, quantized REC, fixed-rate writer, TLA, DC conditioning, and `inop`; native and Rack-linked tests pass. A live Octavia signal check now confirms dry and wet output with an existing Reel. REC-017 snapshot reload awaits Phase 7; recording and subjective audio audition remain untested live. |
| 4 — finite Genes and Morph | **Validation near complete; gate open** | The 48 kHz four-slot engine, primary cursor, natural EOSG, ramp CV, marker capture, queued selection, and PM interaction are implemented and focused-tested. Live Octavia checks cover recording, Shift, Play-stop, PM exit, and the corrected marker handoff. A combined PM/Current/snapshot/reversal fixture and reproducible full-capacity core plus module-callback benchmarks now pass. Formal acceptance matrix coverage, Rack graph/GUI overhead, and subjective audition remain before the phase is declared complete. |
| 5 — transport/options | **Implemented subset; gate open** | Selection, PM, Play/Clock/Vari-Speed modes, single-button release actions, `rsop`, and input gain are implemented and focused-tested. The MVP now has one Reel per module; button chords and Reel Mode are removed from its scope. Options-text transactions and broader same-frame event priority remain. |
| 6–10 | Not started | Phase 7 still requires real Rack GUI end-to-end persistence/duplication tests. |

## Executed commands and outcomes

- Native MINGW64 `make -j10 plugin.dll`: **PASS**, linked `plugin.dll` after an incremental object rebuild. A sandboxed first attempt failed before `make` with Windows CreateFileMapping error 5; the approved native retry succeeded.
- Native MINGW64 `make test-chimera-phase0`: **PASS**. Compiles and runs `tests/chimera_phase0_smoke.cpp` without Rack headers.
- Native MINGW64 `make test-chimera-rack-contract RACK_APP_RUNTIME_DIR="/c/Program Files/VCV/Rack2Pro"`: **PASS**. A stack Rack Engine calls the installed runtime's `prepareSaveModule()` and `prepareSave()` hooks. Plain `dataToJson()` serialization does not call `onSave`, so the probe reports unsaved live state and retains the previous durable revision. This probes the hook contract, not the GUI duplicate or patch archive writer. The test uses internal SDK headers solely to instantiate an Engine outside Rack's application host.
- Native MINGW64 `make test-chimera-rack-save RACK_APP_RUNTIME_DIR="/c/Program Files/VCV/Rack2Pro"`: **PASS**. `tests/chimera_rack_patch_save_spec.cpp` drives the installed Rack patch manager and extracts the resulting `.vcv` archives. It checks autosave without save preparation both before the first bundle and after a newer live edit; a save with matching manifest/audio; a failure before any complete bundle (JSON reports missing audio); alternate-path save failure after new audio is written but before its manifest (archive still references the old complete bundle and reports `saveFailure`); bypassed and stopped saves; and shutdown save preparation. It models the exact source-backed single-module and selection-duplicate API sequences: the former prepares and copies patch storage, while the latter only serializes JSON and needs a plugin-owned immutable cache reference for unsaved audio. The latter two are headless contract prototypes, not GUI actions.
- Native MINGW64 `make test-chimera-phase0`: **PASS** after expansion. `tests/chimera_phase0_capacity_spec.cpp` stresses all 13 jacks changing every 768 kHz host frame across the measured 640-frame input delay. Enqueue-before-consume occupancy peaks at 8,333 of 16,384 records; one 16-frame core burst is 208 records, below the 256 cap. Two 133,632,000-byte active-plus-reserve stores total 267,264,000 bytes, within 256 MiB; a third leased retired store would exceed it and must block preparation.
- Native MINGW64 `make -j10 plugin.dll`: **PASS**, up to date at the final Phase 0 checkpoint.
- Native MINGW64 compile/run of `tests/chimera_speex_link_probe.cpp` with SDK `-lRack` and Rack Pro runtime first on PATH: **PASS**. Quality-5 input latency: 40 host frames at 8/44.1 kHz, 80 at 96 kHz, 160 at 192 kHz, 640 at 768 kHz. This measures library delay, not full Chimera adapter latency.
- Frozen ID fixture compared to specification section 3 with Python: **PASS** (12 parameters, 13 inputs, 4 outputs, 9 lights). The later Phase 3 module now also asserts each Rack ID against the core schema at compile time.
- Native MINGW64 `make -j10 test-fast RACK_APP_RUNTIME_DIR="/c/Program Files/VCV/Rack2Pro"`: **FAIL** at existing `sibyl_module_spec` after many preceding tests passed. The isolated native `.exe` reports two fixture failures: “P5 complete companion expressive-composition fixture compiles” and “P6 combined source compiles.” Both read absent `doc/Sibyl_v3_Example_Composition.json`; that file is absent in this checkout. No Chimera source was involved. The full suite stops there, so later tests were not run.
- `doc/Morphagene-Codex/validate_package.py`: **PASS** after the Phase 0 spec clarification, with unchanged source brief and 138 acceptance cases. This is documentation validation, not module validation.
- Native MINGW64 `make test-chimera-phase1 test-chimera-phase1-fast`: **PASS**. Both C++11 reference-safe and release-equivalent fast-math builds pass the control/core edge tests and all 224 JSON vector evaluations; frozen parameter/input/output/light names and ordering match `tests/chimera_ids_v1.json`. The 10,000-frame callback loop makes no C++ `new`/`new[]` calls and reuses the mapped rate when its smoothed inputs are unchanged. A comprehensive audio-callback allocation/interposition test remains for the full engine.
- Ubuntu WSL `g++ -std=c++11 -O1 -g -fno-omit-frame-pointer -fsanitize=address,undefined -Isrc tests/chimera_phase1_spec.cpp -o build/tests/chimera_phase1_sanitize_wsl && ASAN_OPTIONS=detect_leaks=1 build/tests/chimera_phase1_sanitize_wsl`: **PASS**. MINGW64's GCC 16.1.0 could not link its sanitizer target because `-lasan` and `-lubsan` are absent; the WSL run supplies the bounds/undefined-behavior check for the Rack-independent layer. This is not a substitute for the native Windows plugin build.

## Changed in this phase

- Added `INTEGRATION_BASELINE.md`, this status file, `tests/chimera_ids_v1.json`, `tests/chimera_phase0_smoke.cpp`, `tests/chimera_phase0_capacity_spec.cpp`, `tests/chimera_speex_link_probe.cpp`, `tests/chimera_rack_save_contract_spec.cpp`, and `tests/chimera_rack_patch_save_spec.cpp`.
- Added `make test-chimera-phase0`, `make test-chimera-rack-contract`, and `make test-chimera-rack-save` targets.
- Clarified the spec's 256-event bound by encoding a jack's connection and gate edge together; recorded a 16,384-entry proposed delayed event history based on the measured maximum Speex input delay.

## Handoff and limits

Phase 0 establishes the host contract and a small transactional asset prototype. It does not implement Chimera's production snapshot, workers, patch assets, panel, or audio engine. Once those exist, Phase 7 must exercise real GUI Save/Save As and both Duplicate commands with the installed Rack application, plus patch reload on another machine or an isolated user directory. The headless fixture cannot prove those complete user flows. It also cannot prove that Rack cancels a failed save: the observed behavior is that Rack archives the module's safe fallback JSON. The module must show the failure and keep a coherent prior bundle. An orphan audio file can enter the archive after a manifest-stage failure; it is unreferenced and needs later worker-side pruning.

The unrelated Sibyl fixture failure remains a separate baseline issue. At the Phase 0 checkpoint, no Chimera audio engine, panel or production patch asset implementation existed; Phase 1 adds the isolated control/math core below.

## Phase 1 implementation

- Added `src/ChimeraTypes.hpp` with fixed capacities, all frozen Rack enum IDs, stereo/region PODs, and explicit control/event/output frames. Updated the Phase 0 smoke target to compile these real types.
- Added `src/ChimeraProfile.hpp` with bit-stable finite checks, transfer laws, one-pole and linear fades, Gene hysteresis, finite-duration rounding, cosine-edge windows, region-wrapped cubic interpolation, Morph density, xorshift32 onset choices and stereo balance. The functions express the frozen software profile; they are not hardware measurements.
- Added `src/ChimeraCore.hpp`, a deterministic control/event skeleton. It owns a 64-bit frame counter, double source coordinate, sanitization state, smoothers, seeded onset state and a cached nonlinear rate mapping. Its output currently exposes sanitized monitoring and control state; Reel playback/recording do not exist yet.
- Added `tests/chimera_phase1_spec.cpp`, `tests/chimera_profile_probe.cpp`, and `tools/chimera_phase1_vectors.py`; added Makefile targets for reference, fast-math and sanitizer runs. The sanitizer target is usable in WSL/Linux; the installed MINGW64 toolchain lacks its sanitizer link libraries.

The Phase 1 exit gate is met for its isolated DSP/control scope. CTL-001/002 require a registered Rack module and widget; Phase 3 adds the first development version. DSP playback, storage and transport acceptance cases begin in later phases. No complete Chimera Rack module exists yet.

## Phase 2 synthetic foundation complete

- Added `src/ChimeraReel.hpp`: an off-audio prepared active/reserve page pool; bounded eight-page capture/reclaim; write barrier and one-clone-per-page COW; fixed marker metadata; a published immutable cut; and safe recording stop on reserve exhaustion. The full-capacity synthetic test prepares 32,625 active and 32,625 reserve pages (133,632,000 raw audio bytes), rewrites every frame during capture, compares every frozen frame with the cut, and returns the full reserve after release. Partial last pages and repeated writes to a one-page region are covered.
- Added `src/ChimeraOwnership.hpp`: a non-spinning audio/maintenance token, counted snapshot readers, fixed 64-command and 128-completion SPSC rings (16 completion slots reserved), and service-owned store handles with a 256 MiB raw-payload ledger. A 100,000-command concurrent SPSC run preserved order; a stale host maintenance path completed two cuts without audio callbacks.
- Added `src/ChimeraJobs.hpp` and `src/ChimeraService.hpp/.cpp`: a bounded, lazily started plugin-scoped service with at most two workers, at most 16 unresolved jobs per generation, worker-side store preparation/retirement, and generation-based stale-result disposal. Jobs carry no `Module*` or widget pointer. `plugin.cpp::destroy()` drains the service off audio; merely opening Rack starts no Chimera worker or Reel. The registered module now exercises this service through token-specific polling and a bounded audio handoff.

Ownership flow for the current synthetic layer:

```text
UI/semantic callers --(non-RT dispatcher, later integration)--> service
service --(one producer, 64-entry SPSC)--> audio core
audio core --(one producer, 128-entry SPSC)--> service
worker prepares Reel --> service-owned StoreRegistry --> borrowed audio handle
audio cut/write barrier --> frozen page IDs --> counted worker readers
last reader finishes --> service queues release --> audio reclaims <=8 pages/tick
audio retires old handle --> service destroys store off audio --> ledger credit returns
```

- Native MINGW64 `make test-chimera-phase2`: **PASS**, including the full 174-second capacity case, concurrent snapshot reader versus active writer, 100,000 ordered SPSC commands, stale job cancellation, and budget/handle transitions.
- Native MINGW64 `make -j10 plugin.dll`: **PASS** at the Phase 2 checkpoint, recompiling `src/ChimeraService.cpp` and `src/plugin.cpp` and linking the lazy service into the Windows plugin. Model registration came later in Phase 3.
- Ubuntu WSL ASan/UBSan builds of `tests/chimera_reel_spec.cpp`, `tests/chimera_reel_concurrency_spec.cpp`, `tests/chimera_jobs_spec.cpp`, and `tests/chimera_service_lifecycle_spec.cpp`: **PASS**. The installed Windows MINGW64 GCC lacks `-lasan`/`-lubsan`.
- Ubuntu WSL GCC and Clang TSAN binaries initially **failed before test execution** with `FATAL: ThreadSanitizer: unexpected memory mapping`; even a one-line `main()` reproduced this. In Ubuntu 22.04 under WSL kernel 6.18, a per-process `setarch x86_64 -R` allowed the minimal binary to start. GCC 11 TSAN runs of `chimera_reel_concurrency_spec`, `chimera_jobs_spec`, and `chimera_service_lifecycle_spec` then **passed** with `-fsanitize=thread -fPIE -pie -O1 -g`; no races were reported in those exercised paths. This does not cover a registered module or every possible interleaving.

The registered module's control dispatcher now polls only its generation token, admits a worker-prepared Reel to the ledger, sends a pointer-bearing POD command to audio, and waits for audio acknowledgment before detaching the old store for worker retirement. The old payload remains charged until retirement completes. On module removal, queued/completed jobs are discarded and an in-flight worker self-retires without accessing a Module or Widget. The Rack-linked test covers a real module swap, full-capacity preparation canceled during removal, and zero heap allocations in initial adoption, recording, and swap callbacks. Updated jobs/service tests pass native, ASan/UBSan and TSAN (using the WSL `setarch` workaround).

The module now creates neither a full Reel nor a worker when untouched. First audio connection or REC requests full-capacity worker preparation through the control dispatcher; an early REC is rejected visibly and must be pressed again after readiness. The core ownership token gates audio callbacks and stopped-host maintenance. Snapshot begin, ready, release and reclamation use bounded commands/completions; worker-facing readers see only frozen pages. The Rack-linked fixture proves an active overwrite leaves the frozen cut unchanged, a second reader delays release, and a stopped host can capture/reclaim and then make a newer cut without callbacks. This completes the Phase 2 synthetic foundation. Production SaveBundle fencing, encoding and durable patch assets remain Phase 7, so the fixture is not evidence of a saved recording.

## Phase 3 first registered slice: vertical milestone complete, formal gate partial

- `src/Chimera.cpp` registers exactly one `modelChimera`; `plugin.json` has one matching `Chimera` entry. The temporary `res/Chimera.svg` panel exposes S.O.S., Vari-Speed, Slide, Record, stereo input/output, PLAY/CLOCK/REC gate inputs, CV OUT, EOSG, and context actions for Append and live-only writer routing. It states that audio is not saved. The full profile-1 parameter/port/light IDs are configured and compile-time checked; most remain off this developer panel until Phase 8.
- `src/ChimeraSlice.hpp` is the Rack-independent 48 kHz slice. It reads a bounded region through cubic taps before writing, advances Current/Append by one frame per core tick regardless of Play or rate, fades writer source over 48 frames on an `inop` change, applies default 5 Hz input/output DC conditioning with a test bypass, and keeps the initial Append inaudible until finalized. Full-capacity pages are prepared lazily on the worker after first connection or record request. Worker-prepared replacement and retirement use the same handle path; snapshot leases now cross the module's ownership boundary.
- The user confirmed on 2026-09-23 that Chimera appears and loads in Rack. The developer panel is intentionally incomplete; the production interface is scheduled for Phase 8. Play rise restarts from the Slide origin (reverse starts at origin minus one), natural full-Splice completions are counted at the next frame entry with fractional travel retained, and the float32 default 5/6 Vari-Speed control snaps to exact 1×.
- Native `make test-chimera-phase3`: **PASS** for initial capture, fixed Current advancement and wrap, Append finalization, exact unconditioned TLA recurrence, `inop` transition, DC conditioning, Play retrigger, and full-Splice boundary timing. Native Rack-linked `make test-chimera-module RACK_APP_RUNTIME_DIR="/c/Program Files/VCV/Rack2Pro"`: **PASS** for model slug/schema, mono monitoring, record/stop frame inclusion, honest JSON, non-48 kHz guard, worker-prepared swap/retirement, removal cancellation, and callback allocation traps. WSL Ubuntu ASan/UBSan of the isolated slice and final jobs test: **PASS**; GCC TSAN of updated jobs/service tests with `setarch x86_64 -R`: **PASS**. Native `make -s -j10 plugin.dll`: **PASS** after the service handoff edits. `python tools/validate_plugin_json_tags.py plugin.json`: **PASS**.
- The Phase 3 milestone covers the 48 kHz full-Splice slice, not the eventual complete instrument. At other host rates it passes live input through, clears CV/EOSG, stops recording and flags an error. It has no production audio persistence; JSON records `audioStatus=unsaved-development-slice`. Finite/high-Morph feedback cases belong to Phase 4, host-rate alignment to Phase 6, snapshot reload to Phase 7, and complete Play modes/production UI to later phases. A live Rack audio/interaction check has not yet been performed for this revision.
- The 48 kHz development slice now emits a 0/10 V EOSG pulse on natural full-Splice completion, with a width capped at 240 frames and shortened below half the observed/expected traversal interval; stopping Play clears it. CV OUT now exposes the specified attack/release energy follower of conditioned stereo output, with a two-step fast square-root approximation. The Rack module applies 2.5 V/1.0 V Schmitt handling to PLAY and REC gates and clears CV/EOSG on its unsupported-rate fallback. Focused native slice and Rack-linked module tests pass, including an envelope reference comparison within 1 mV and callback allocation checks; native `plugin.dll` links. Finite-Gene/chord EOSG, CV ramp mode, host-rate alignment, and full Play modes remain later work.
- The final Phase 2/3 pass adds a module-level snapshot command/lease path, CoreOwnership around the audio callback, stopped-host capture/reclaim, and true first-use Reel preparation. Phase 3 gains Clock-armed Current/Append start/stop with REC-before-Clock same-frame ordering, latched writer region, a nonfinite stored-sample guard, and the five added developer-panel jacks. The expanded slice tests cover a one-frame TLA recurrence, 298/299/300 marker boundaries, the final Append frame, exact 4800-frame traversal timing at 0.5×/1×/2×, fixed writer advancement across rate direction and Stop, and the 48-frame wet Stop fade. The Rack-linked fixture covers empty-module laziness, early REC rejection and retry, gate hysteresis, quantized record frame inclusion/exclusion, snapshot overwrite/reader/release, successive stopped-host cuts, and no callback allocations. Native Phase 2/3 and Rack-linked tests, WSL slice ASan/UBSan, WSL ownership TSAN with `setarch x86_64 -R`, and native `plugin.dll` all pass.


## Phase 4 granular-playback checkpoint (gate open)

- Added `src/ChimeraGrains.hpp`, a Rack-independent four-slot engine for both finite Genes and full-Splice playback. Finite duration is latched in core frames; full-Splice musical voices expire by their own absolute effective-rate source travel. A separate non-reading primary cursor/cycle follows base rate only, preserving fractional full-Splice excess and resolving at most one boundary per frame. A fixed onset-phase accumulator implements Morph density, with two xorshift draws per onset and signed chord ratios latched in the engine. Each musical slot has one 48-frame outgoing cursor; repeated replacement uses a bounded scalar fade. The reader ceiling is eight. A 2,049-entry half-cosine table is built outside the audio callback and linearly interpolated in the callback. The finite density estimate is refreshed at most once per 32 core frames, while new voice duration is latched at onset.
- `ChimeraSlice` now uses the bounded musical engine at both Gene modes. Its independent Current/Append writer and TLA path remain unchanged. Natural voice completions drive EOSG; the primary cycle drives a 0–8 V ramp CV option. Gene Size and Morph knobs are accessible on the temporary panel, with persisted `gnsm` smooth-window, `cvop` ramp, `omod` immediate-transition, and signed `mcr1..3` ratio controls in the context menu. High Morph defaults to ratios 2, 3, 4. Invalid ratio values in patch JSON retain the prior valid value; a dedicated corrupt-document warning is still future work.
- Native MINGW64 `make test-chimera-phase1 test-chimera-phase4 test-chimera-phase3 test-chimera-module RACK_APP_RUNTIME_DIR="/c/Program Files/VCV/Rack2Pro"`: **PASS**. Focused cases cover 480-frame finite completion at 0.5×, 1×, 2×→−0.5× and −1× with distinct source travel; a full-capacity finite/full side-by-side fixture; full-Splice source-travel expiry and signed secondary completion without primary reset; L=1/2/3 at 32×; four-slot onset cadence and eight-reader bound; constant-source unity plateau; deliberate smooth-mode dips and below-unity no-voice gaps; discontinuous-source causal residual and `omod` bypass; signed slot ratios; table versus reference window error below 1e-5; nonfinite source containment; Slice ramp/EOSG boundary timing; and Rack callback allocation traps while finite and full high-Morph playback run.
- The extended isolated fixture also runs every Morph density anchor for ten core seconds, compares PRNG state against exactly two draws per onset across low/high Morph, verifies new ratio settings affect only later onsets, and checks `gnsm` policy remains latched on existing voices. These tests are source-backed profile checks, not a listening or hardware calibration result.
- WSL Ubuntu ASan/UBSan of `tests/chimera_grains_spec.cpp` and `tests/chimera_slice_spec.cpp`: **PASS**. Native MINGW64 `make -j10 plugin.dll`: **PASS** after the latest edits. The subsequent live Rack signal check is below; subjective listening has not yet validated the new controls.
- **Gate remains open.** The subsequent section 8.4 reader-tail fix is recorded below. REC-018's marker capture across overlap, gaps and Stop awaits Shift/selection behavior in Phase 5. The full DSP/OUT acceptance matrix, performance measurements, and live recording/clock/transition checks remain. Do not describe Phase 4 as complete or advance the formal Phase 5 gate on this evidence.

## Live Rack/Octavia signal check, 2026-09-24

Rack Pro was running the development Chimera in `Chimera-Testing.vcv` at 48 kHz. The existing patch routed Bifurx mono audio into Chimera's left input, Integral Flux into PLAY, and Chimera's left output to MixMaster. Four temporary Octavia probes were connected to Chimera L/R, Bifurx source, and Chimera EOSG. These probes measured the actual cabled ports; they were removed after the check. All five changed Chimera controls were restored to their original values, and the patch was not saved. The running module reported `audioStatus=unsaved-development-slice`; no recording, reload, or duplication was attempted.

- At the original controls, a 3-second capture measured Chimera L/R at 0.926 V RMS each, with identical channels and no clipped audio samples; the Bifurx source was active. EOSG was low over this short full-Splice window.
- With S.O.S. at 0, a 1-second capture measured active dry L/R at 2.497 V RMS each. With S.O.S. at 1, full-Splice playback remained active at 0.541 V RMS over 2 seconds. This checks live dry and wet routing, but does not claim bit-identical bypass or recorded-audio durability.
- With finite Gene Size 0.55, forward Vari-Speed, and Slide at 0, nominal Morph (1/6) produced identical L/R at 0.689 V RMS; maximum Morph produced L 0.408 V RMS, R 0.383 V RMS, stereo correlation 0.692, and no clipped audio samples over 2 seconds. EOSG pulsed between 0 and 10 V in both finite-Gene captures. At the patch's original Slide position (~0.723), the same finite-Gene setting produced silence while EOSG continued; returning Slide to 0 restored audible signal. The user confirmed that the recorded Reel pulses, so a quiet source interval is expected at some origins.

In a follow-up on the same running patch, Iris's continuously active wavetable output was temporarily substituted for Bifurx at Chimera's left audio input without changing the existing Reel. With S.O.S. dry and the original Vari-Speed, a 2-second capture found identical L/R at 3.287 V RMS, near-zero output DC, and no clipped audio samples. The unconditioned Iris source had a substantial DC offset, so this also exercises Chimera's input/output DC conditioning. CV OUT held approximately 8 V at this high signal level. At Vari-Speed Stop with S.O.S. fully wet, a 1-second capture showed exactly 0 V on L/R and CV OUT while Iris remained active at 4.762 V RMS. Returning S.O.S. fully dry while still stopped restored identical L/R at 4.004 V RMS and CV OUT at approximately 8 V. This checks that Stop silences wet playback without stopping dry monitoring; it does not measure the 48-frame fade transient. The original Bifurx routing/color and controls were restored and the four temporary monitor cables removed. The patch was not saved.

This is a host-level electrical/signal integration check, not a subjective listening review or a complete Phase 4 acceptance pass. CV output modes, recording, clocked behavior, Play transitions, host-rate conversion, save/reload, and long-run performance were not exercised in this session.

## Phase 4 forced-transition reader fix, 2026-09-24

The scheduler previously discarded all readers on a Play retrigger or direct region selection, and replaced a finite/full mode change with a scalar output residual. It now moves each outgoing musical voice into its fixed 48-frame transition reader, preserving that voice's captured source region while the new voice starts. A repeated replacement converts an already-active tail to its bounded scalar emergency fade. The `omod=1` immediate option still cuts directly with no added selection crossfade. Forced tails do not count as natural EOSG completions, and the limit remains four musical voices plus four transition readers. `Slice::selectRegion()` no longer resets the scheduler before the new region can render.

Native MINGW64 `make test-chimera-phase4 test-chimera-phase3 test-chimera-module RACK_APP_RUNTIME_DIR="/c/Program Files/VCV/Rack2Pro"`: **PASS**. New deterministic tests cover retained readers across a Play retrigger and finite/full mode change, the exact 48-frame retirement, repeated retriggers with bounded scalar fallback, old-region reads during direct selection, immediate-mode bypass, and the selection path through `Slice`. Native MINGW64 `make -j10 plugin.dll`: **PASS** after recompiling Chimera and linking the Windows DLL. WSL ASan/UBSan runs of the updated `chimera_grains_spec.cpp` and `chimera_slice_spec.cpp`: **PASS**. These are source/fixture results. The already-running Rack instance has not loaded this rebuilt DLL; no plugin install or Rack restart was performed because its unsaved development Reel would be lost. Queued selection, complete Play modes, remaining Phase 4 cases, performance targets, and a live audition of this fix remain open.

## Native Linux Phase 4 verification, 2026-09-24

The active development host changed to native Ubuntu Linux (kernel 6.8, `x86_64-linux-gnu`, Intel Core Ultra 7 165H) with the matching `../Rack-SDK`. This checkpoint uses the Linux compiler and `plugin.so`; the preceding MINGW64 results remain historical evidence for the earlier Windows checkout.

- Expanded the focused fixtures for independent stereo center/hard-side balance, centered overlapping-voice normalization, full-Splice Stop address/phase and onset behavior, finite-Gene timer behavior at Stop, post-S.O.S. stereo CV energy, the 1 kHz sine follower's reference mean/min/max, the 54-frame maximum-Morph EOSG pulse, finite CV ramp during secondary overlap and reverse playback, and full-Splice CV ramp under Vari-Speed Stop versus stopped transport. `make test-chimera-phase4 test-chimera-phase3` and Rack-linked `make test-chimera-module`: **PASS**.
- Linux ASan/UBSan executions of the updated grains and slice fixtures: **PASS** with leak detection disabled. LeakSanitizer could not initialize in this traced environment; this does not establish a leak check.
- Forced recompilation of `src/Chimera.cpp` and full native Linux `plugin.so` relink with `make -W src/Chimera.cpp -j10 plugin.so`: **PASS**.
- A focused optimized offline `Slice` benchmark on the Core Ultra 7 165H processed 480,000 core frames with a 4,800-frame Reel, maximum Morph, 480-frame finite Genes, S.O.S. feedback and Current recording in 63.1–66.3 ms across four sequential runs (0.63–0.66% of one core for this fixture). This is not the release target measurement: it omits PM, full-capacity pages, snapshot copy-on-write, Rack callback overhead, and host-rate SRC. No performance conclusion about those paths is recorded.

Phase 4 remains open. REC-018 marker capture and DSP-018 queued selection require Phase 5 selection/Shift semantics; PM-dependent DSP-015/017/020 also await Phase 5. The remaining natural EOSG event matrix, long-run/performance stress, and live audition of the rebuilt transition behavior are still unverified.
The Octavia bridge was unreachable on this native Linux session because Rack was not running; no live signal or audition claim is made for this checkpoint.

## Live Linux Rack transition finding and fix, 2026-09-24

Rack was subsequently started with Chimera and Octavia. Its Audio-2 device initially drove the engine at 96 kHz, where this development Chimera correctly uses its live-input fallback. After the active output was set to 48 kHz, a temporary Fundamental VCO sine was recorded into Chimera's initial Reel. With the VCO input cable removed and S.O.S. at full wet, Octavia still measured approximately 3.65 V RMS on both output channels with no clipped audio samples, confirming live capture/playback. Finite Gene Size 0.55 and maximum Morph produced 1.82/1.92 V RMS left/right, 0.646 stereo correlation, and 10 V EOSG pulses. These measurements establish host signal flow, not subjective sound quality or durable saved audio.

A frame-synchronized Octavia Control A test held PLAY high and drove it low for 48 frames at capture offset 24,000, then returned high. The old running DLL emitted a one-sample wet-output drop at the low edge: 3.143 V on left and 4.441 V on right in the captured waveform. `Slice` set `canRead=false` and zeroed `wet` before multiplying by its 48-frame `wetGain_`, so that gain ramp could not soften a stopped transport. The source was a saved 48 kHz stereo Octavia capture (`chimera-phase4-play-retrigger`, recording 6); the reported audio clip count was zero, but the edge itself was a clear click risk.

`ChimeraSlice.hpp` now carries the last audible wet sample through a fixed 48-frame scalar stop tail and blends it with a new reader when PLAY returns early. A deterministic regression checks every stop-frame value, immediate EOSG clearing, and a one-frame low/high return. Linux `make test-chimera-phase3 test-chimera-phase4 test-chimera-module`, updated slice ASan/UBSan (with leak detection disabled as above), and native `make -j10 plugin.so` all **PASS**. The rebuilt binary was atomically placed in the disposable Rack plugin install with the previous binary backed up under `/tmp`.

After Rack restarted at 48 kHz, a fresh VCO Reel was recorded and verified at full wet mix with its input disconnected. Repeating the identical Octavia Control A low pulse at capture frame 24,000 produced one-sample steps of 0.068 V left and 0.003 V right at the former click frame, versus 3.143/4.441 V before the fix. Across the 22-frame edge neighborhood the new maximum was 0.182 V left and 0.021 V right, below the capture's ordinary outside-window p99 steps of 0.469/0.474 V. The subsequent PLAY rise likewise showed no exceptional jump in the checked return neighborhood. This is a live signal/click verification of the reloaded binary, not a subjective listening review. Octavia's `chimera-phase4-play-retrigger-after` 48 kHz stereo WAV and sidecar record the executed control frame; no patch save was made.

## Primary-boundary selection subset, 2026-09-24

Added a bounded `Selection` arbiter with equal Organize bins, 10%-of-bin hysteresis, separate requested and committed Splice indices, and Shift that persists while the knob remains stationary. The 48 kHz Slice commits a request at the primary boundary by default, immediately under `omod=1` or stopped transport, and before a Play retrigger. High-ratio secondary completions do not commit selection. A running Append finalization now requests its new Splice without cutting off old playback; a stopped Append can select it immediately. The module resolves SHIFT button release and jack rise, with Schmitt handling and a pending light. No new enum IDs were inserted.

Focused tests cover Organize dither/large jumps/end bin, same-frame Organize-before-Shift arbitration inside the selection class, stationary-knob Shift behavior, secondary completion before primary commitment, immediate-mode hard selection, Append auto-request, and retrigger to the selected region's Slide origin. Rack-linked tests cover button release, jack rise/jitter, and pending light. Native Linux `test-chimera-phase1`, `test-chimera-phase3`, `test-chimera-phase4`, `test-chimera-module`, updated Slice ASan/UBSan (leak detection disabled), and full `plugin.so` compilation/link: **PASS**. The focused optimized 480,000-frame high-Morph/Current benchmark took 73.8–84.0 ms per 10 seconds (0.74–0.84% of one core) across four runs on the Core Ultra 7 165H; the same omitted release-target paths listed above still apply. This newly linked binary has not been installed/reloaded into the running Rack session.

The Phase 4 gate is still open for PM-dependent DSP-015/017/020 and the remaining acceptance/performance matrix. The broader Phase 5 event ordering and Clock/Play modes are also open; this section is an implemented subset, not a claim of Phase 5 completion.

## SPLICE marker capture checkpoint, 2026-09-24

SPLICE button release and Schmitt jack rise now request a marker. The 48 kHz Slice takes the Current writer's pre-write address, the playing primary cursor after Slide but before its frame advance, or the stopped cursor. Append endpoint markers are inserted only after their addressed frame is written, while reserving a slot for the Append-start marker. Duplicate addresses are harmless. Marker IDs keep current and requested selections stable when sorted insertion changes their indices. Marker edits freeze the old playback bounds until the primary boundary, while already launched voices retain their own region; Append finalization requests its first appended marker even when interior markers exist.

Native Linux `test-chimera-phase2`, `test-chimera-phase3`, `test-chimera-phase4`, Rack-linked `test-chimera-module`, Slice ASan/UBSan with leak detection disabled, and full `plugin.so` link passed for this checkpoint. Deterministic fixtures check live pre-advance address, continuing old-region audio after a split, queued boundary activation, stopped cursor/duplicate behavior, Current pre-write capture and stable ID remap, Append staging/finalization, and Rack button/jack edges. The full `test-fast` suite stopped at an unrelated localhost-port allocation failure in the sandbox. The newly linked binary has not been installed in the running Rack, so there is no live SPLICE signal or listening result yet. REC-018 still needs finite overlap/gap and long-run live acceptance checks before closing the formal gate.

## Live Linux SPLICE and Shift check, 2026-09-24

The rebuilt, stripped Linux `plugin.so` was installed into the disposable Rack plugin directory after a full native link, and Rack was restarted at 48 kHz with Octavia active. A Fundamental VCO sine was recorded into Chimera's initial Append: roughly 262 Hz in the first segment and 523 Hz after a SPLICE button release during recording. The source cable was removed and S.O.S. set fully wet. Octavia monitor A measured 3.65 V RMS with no clipped samples, and its 500 ms spectrum showed the lower fundamental near 258 Hz. Setting Organize to its last bin yielded the higher fundamental near 527 Hz. A SHIFT button press held for 120 ms and then released returned the output to the lower fundamental, while Organize remained at its last-bin setting. This verifies a real recorded marker, two-region selection, and stationary-knob Shift behavior in the reloaded Rack build. An immediate bridge press/release did not change playback; it was too brief to establish a reliable Rack audio-thread button event, and is not counted as a failed Chimera transition. Octavia snapshots 1, 3, and 4 contain the lower, higher, and returned lower-tone measurements. The patch was not saved.

This live pass verifies two audible regions and selection after settling. It does not yet measure the exact frame of boundary commitment, a finite-Gene overlap/gap marker, or a subjective listening result. The sandboxed `test-fast` run stopped on an Octavia test's localhost socket creation: a separate minimal Python socket attempt returned `PermissionError: Operation not permitted`, confirming an environment restriction rather than a Chimera test failure. An attempted outside-sandbox rerun was interrupted before it returned a test result.

## Finite marker coverage and PM subset, 2026-09-24

Deterministic 48 kHz Slice tests now capture markers with overlapping/chord finite Genes and during a true low-density no-voice gap. Different signed chord ratios leave the Slide-adjusted primary address unchanged; the gap capture retains that address without borrowing a musical reader. These extend REC-018 coverage beyond the prior full-Splice/Stop tests. The exact live frame of marker capture and a longer mixed-mode run remain unmeasured.

Added a `pmin` context option and persisted setting. A 10 ms raw-left energy detector enters PM only after 144,000 quiet frames with R connected and exits after 16 frames above the upper threshold, or when R is unplugged/PM disabled. Its 240-frame blend removes R from live monitor/record routing and applies up to 96 source frames per right-input volt, clamped at ±10 V, to all musical and transition reads. The base cursor, primary cycle, marker address, and Current writer coordinate remain unmodulated. PM can keep a stationary full-Splice reader audible at Vari-Speed Stop; the existing wet Stop fade resumes after PM exits. The Rack module drives the PM light from the blend and clears it on unsupported host rates.

Focused Slice tests cover entry timing, Stop audio, an unmodulated marker address, right-input exclusion from a Current recording, left-signal exit and returned Stop silence. A Rack-linked module test uses a physically connected quiet L input and R signal to verify presence entry, PM light/output, JSON persistence, and left-signal exit. The grains fixture stresses ±960-frame offsets, 32× base rate, ±16 secondary ratios, L=1/2/3, and Stop without extra onsets or invalid reads. Native Linux `test-chimera-phase3`, `test-chimera-phase4`, `test-chimera-module`, Slice and grains ASan/UBSan with leak detection disabled, and full `plugin.so` link: **PASS**. The focused optimized offline max-Morph/PM/Current benchmark used a 4,800-frame Reel and took 134.8–174.5 ms per 10 seconds (1.35–1.75% of one core) on the Core Ultra 7 165H; it omits Rack overhead, full-capacity pages, host SRC, and snapshots. The new PM binary has not been installed in the still-running Rack session, so PM has no live or listening verification yet.

## Live Linux PM signal check, 2026-09-24

The rebuilt PM plugin was installed atomically in the disposable Rack plugin directory and Rack restarted at 48 kHz with Octavia active. A fresh roughly one-second VCO sine Reel was recorded through L. With S.O.S. fully wet, Octavia measured 3.65 V RMS on each Chimera output and no clipped samples. After the source was moved to R and Vari-Speed set to Stop, active PM drove 3.46 V RMS on both outputs. Unplugging R brought both outputs to exactly 0 V in a 200 ms snapshot. Reconnecting R restored the modulated Stop sound after the quiet-left dwell. Patching the still-active VCO into L caused PM to exit and both wet outputs to return to 0 V, while R remained connected.

With S.O.S. fully dry, connected active L and R yielded 3.65 V RMS on both outputs. After L was unplugged and PM re-entered, the dry outputs measured 0 V even while Octavia's separately cabled source monitor C measured 3.66 V RMS. This verifies that R is removed from live monitoring when assigned to PM. The fixture's Current-writer exclusion and exact detector timing remain test evidence rather than live readback, and this is an electrical signal check rather than subjective listening. No patch was saved. The Rack test rack remains at Stop with R patched as the PM source and S.O.S. restored fully wet.

## Phase 4 combined stress and PM exit transition, 2026-09-24

A newer live Rack session was running the same PM build at 48 kHz, but its unsaved Reel had been cleared on restart. After recording a fresh roughly one-second VCO sine, the VCO stayed on R as a PM source, Octavia Control A supplied a quiet connected L, and Chimera was held at Vari-Speed Stop with S.O.S. wet. A pre-capture snapshot measured 3.66 V RMS on both outputs. An Octavia bounded capture applied a +5 V Control A event to L at exact capture frame 48,000 for 24,000 frames. The event executed at Rack frame 9,598,528; the 120,000-frame four-channel WAV and sidecar are labeled `chimera-phase4-pm-exit-step` in Octavia Recordings. The first output step at the trigger was 0.294 V on each channel, below the pre-event waveform's 99th-percentile ordinary step of 3.68 V. Ten-millisecond output RMS fell from 3.22 V at the event to 0.52 V at +10 ms, 0.031 V at +100 ms, and about 0.0013 V at +200 ms; no output samples clipped. The long tail includes output DC filtering, so these numbers do not isolate the 240-frame PM blend alone. The temporary Control A cable was removed; the patch was not saved.

An added deterministic Slice stress case combines PM at maximum Morph, Current recording, alternating right-input displacement, repeated forward/reverse rate changes, and a frozen Reel snapshot. All output remained finite; 19 active pages were copied on write in the small fixture, frozen frame 2000 retained its original sample while the active frame was overwritten, and all reserve pages returned after release. Native `test-chimera-phase3` and Slice ASan/UBSan (leak detection disabled) pass with this case. A full-capacity optimized offline benchmark prepared 8,352,000 valid frames and a 32,625-page active/32,625-page reserve Reel (267,264,000 raw audio bytes), then ran four 480,000-frame high-Morph/PM/Current passes with a full-length snapshot capture/release and 392 COW page copies in each pass. On the Core Ultra 7 165H, runs took 93.0–111.6 ms per 10 seconds of core audio (0.93–1.12% of one core). Across 480-frame blocks, the slowest run's p95 was 0.161 ms and p99 was 0.195 ms per 10 ms block. This remains an offline core fixture: Rack callback overhead, full host-rate conversion, simultaneous GUI work, and subjective audition are outside the measurement.

The benchmark source is now reproducible as `tools/chimera_phase4_bench.cpp`, with its Linux compile command in the file. A repeat run of that exact source took 89.4–93.7 ms per 10 seconds (0.89–0.94% of one core), with slowest-run 480-frame block p95 0.115 ms and p99 0.146 ms; each run again made 392 COW copies. The original and repeat figures are both retained to show short-run variability. The benchmark includes a per-frame volatile checksum and per-block clock reads, so it is a conservative core fixture rather than a process-wide Rack measurement.

A second reproducible fixture, `tools/chimera_phase4_module_bench.cpp`, includes `Chimera::process()` and its output writes while running the same full-capacity PM/Current/high-Morph snapshot workload. Four native Linux runs took 103.5–109.5 ms per 10 seconds (1.03–1.10% of one core); the slowest observed 480-frame block p95 was 0.163 ms and p99 was 0.198 ms. Every run made 392 COW copies. This covers the module callback wrapper but still excludes Rack's graph scheduling, concurrent GUI, and non-48 kHz bridge work. Both benchmark sources and exact compile commands are retained in the repository.

## Marker metadata boundary correction, 2026-09-24

The Phase 4 acceptance review found that committing a marker table change at a finite-Gene primary boundary also forced an immediate Morph voice launch. With a fractional onset hop, that extra launch replaced an overlapping old voice before its scheduled end. `Grains` now resets the primary cycle to the refreshed region while retaining the onset phase, active slots, their captured regions, and their ages. The next scheduled onset adopts the new region. Slot Slide motion wraps within each slot's captured region; full-Splice metadata handoff retains fractional source-travel excess. A deterministic two-region regression verifies no extra onset or reader change at the boundary, identical old-region audio until the next natural onset, then new-region audio at the unchanged cadence.

Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-module plugin.so`: **PASS**. Updated Slice and grains ASan/UBSan fixtures: **PASS** with leak detection disabled because LeakSanitizer cannot initialize in this traced environment. `git diff --check`: **PASS**. At this checkpoint the disposable Rack still had the preceding PM build; the later live result appears below. Phase 4 remains open for the formal acceptance matrix and host-level performance/listening check.

## Live Linux marker handoff check, 2026-09-24

The corrected `plugin.so` was loaded into a restarted disposable Rack at 48 kHz. After a fresh VCO Reel was recorded with dry input and S.O.S. returned fully wet, Octavia measured 3.563 V RMS per Chimera output over 0.5 seconds. With finite Gene Size 0.55 and Morph 0.7, a SPLICE button press/release during a three-second Octavia recording shortened the active region at a later primary boundary. Output RMS settled near 1.44 V after the handoff. The largest adjacent-sample change in either audio channel over all 144,000 captured frames was 0.203 V; the pre-event sine already reached the same maximum step. No audio sample clipped. EOSG was recorded on Octavia D. The WAV and sidecar are `octavia-1790263882700-4-chimera-phase4-marker-handoff-live` under Octavia Recordings.

This establishes live audio continuity and that the new marker table reached playback. The temporary development panel has no SPLICE gate port widget, so the button was actuated by a Rack parameter write rather than a frame-synchronized Octavia control event. The capture therefore does not prove the exact commitment frame or onset count; the deterministic grains regression does. The unsaved test patch was not saved. Phase 4 remains open for the formal acceptance matrix, Rack graph/GUI performance, and subjective audition.

## Phase 5 Play transport modes, 2026-09-24

Added persisted `pmod` 0/1/2 and a context-menu selector. The module now separates the PLAY jack's Schmitt level from the actual transport state. In default mode, a fall requests a stop at the next primary boundary and a rise cancels that request and retriggers. Mode 1 stops immediately with the existing 48-frame wet tail. Mode 2 ignores falls while running and retriggers once on a rise. Patched low at initialization stays stopped; removing a low cable makes one normal-high rise, while inserting an already-high cable creates no extra retrigger. Invalid persisted mode values are ignored.

Rack-linked tests cover startup low, boundary stop, pending-stop cancellation, mode-2 low/rise, high-cable insertion, existing immediate-stop Schmitt behavior, and JSON persistence. Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-module plugin.so`: **PASS**. At this checkpoint the binary had not yet been installed into the running Rack; the later live comparison appears below. The Play transition event matrix still needs the exact coincident primary-completion/Clock/REC ordering check, and Clock modes, chords, Reel Mode, rsop, gain switching, and options transactions remain open Phase 5 work.

## Live Linux Play-mode listening fixture, 2026-09-24

The Phase 5 binary was loaded into a disposable 48 kHz Rack. Chimera's unsaved Reel was filled from a Fundamental VCO sine with `inop=1`, then the REC toggle was pressed again to end Current recording. Octavia A/B monitor the stereo wet output, C the source VCO, and D EOSG. The first attempted comparison used an unexpectedly long Reel because REC release does not stop recording; its full-Splice boundary fell beyond the four-second low interval. A measured EOSG period of 11,612 frames at Gene Size 0.4 established the Reel length indirectly, after which Gene Size 0.27 produced 48,387-frame (about 1.008 s) primary cycles. The later comparisons below use this measured finite Gene and a stable roughly 3.65 V RMS wet signal.

Octavia Control A was temporarily connected to PLAY and delivered the same bounded program in each mode: +5 V high, 0 V from capture second 2 through second 6, then +5 V again. The three ten-second stereo/EOSG captures are labeled `chimera-pmod0-listen-confirm`, `chimera-pmod1-listen-confirm`, and `chimera-pmod2-listen-confirm` under Octavia Recordings. In `pmod=0`, output first reached a quiet 10 ms bin at 2.95 s, close to the next primary boundary; it restarted at 6 s. In `pmod=1`, the output reached a quiet bin by 2.02 s and restarted at 6 s. In `pmod=2`, it stayed at about 3.66 V RMS during the full low interval and retriggered at 6 s. No clipping was observed in the monitoring captures. The Mode 2 high-edge waveform has a 1.874 V adjacent-sample step against a 0.216 V outside-window p99 step; this is a potential retrigger click to investigate with the user's listening feedback and a focused transition fixture. These are electrical measurements, not a subjective sound judgment.

The temporary Control A→PLAY cable was removed after the comparison. The live unsaved patch remains on `pmod=2` with an unpatched normal-high PLAY input and a 3.65 V RMS wet loop. The current temporary panel does not make the REC recording state clear to the user; that UI limitation was noted during this setup. No patch was saved.

## Live loop-pop diagnosis and test-Reel repair, 2026-09-24

The user heard a periodic pop during the steady listening loop. A five-second Octavia A/D recording (`chimera-loop-pop-diagnosis`) showed an 8.566 V adjacent-sample jump every 48,387 frames, always 17,663 frames after EOSG rather than at the primary Gene boundary. The repeated fixed offset identified a discontinuity in the test Reel, consistent with the earlier REC-toggle setup having left mixed silent/recorded material. With `inop=1`, a new Current pass overwrote the first Gene from the live VCO and a second REC press ended the pass. The subsequent five-second recording (`chimera-loop-pop-after-rewrite`) kept the 48,387-frame EOSG cadence, 3.651 V RMS output, and reduced the largest adjacent-sample change to 0.217 V, the ordinary sine-step magnitude. This repair removed the sharp repeating pop, while the separately measured Mode 2 retrigger edge remains an open Phase 5 transition check.

The user reported that the rewrite sounded much cleaner but left a smaller periodic blip. Analytic-envelope inspection of the same capture found a roughly 5 V sine amplitude falling to 0.568 V immediately after each EOSG boundary without a sharp adjacent-sample jump. Octavia C measured the source VCO at 261.626 Hz; the 48,387-frame Gene held 263.735 cycles, so a wrap necessarily changed phase. Gene Size was adjusted from 0.270000 to 0.269909, producing an observed 48,436-frame period (approximately 264 cycles). In `chimera-phase-aligned-gene`, the first 100 post-boundary frames averaged 5.098 V analytic amplitude, with a 4.933 V minimum, versus the prior 2.374 V average and 0.568 V minimum. The maximum adjacent-sample step stayed at the ordinary sine value, 0.217 V. The user then reported that the phase-aligned loop sounded clean. It remains playing in the unsaved test rack. This is a listening-fixture calibration, not a change to the specified 96-frame unity-boundary residual algorithm or a claim that arbitrary phase-misaligned loops become inaudible.

The Phase 4 Profile 1 contract in section 8.5 explicitly specifies the 96-frame causal residual shape and says changing source material may retain a derivative or timbral transition. The captured misaligned-sine dip is therefore a measured quality limit of its unity-density setting, not evidence that its boundary correction failed. No default DSP change is made here. Morph's existing overlap behavior must be evaluated before considering any new seam mode.

## Live Morph gap/unity/overlap comparison, 2026-09-24

The user correctly pointed out that Morph itself is the intended performance control for gaps, continuous unity playback, and blended overlaps. On the same phase-misaligned VCO Reel (Gene Size 0.27), three four-second Octavia A/D captures were made with Morph 0, 1/6, and 0.5. At Morph 0, 40 of 400 ten-millisecond bins had RMS below 0.1 V, confirming deliberate no-voice gaps. At 1/6, none were quiet, but the first 100 frames after a primary boundary had a 0.568 V analytic-envelope minimum caused by the phase mismatch. At 0.5, no bins were quiet and the sampled completion neighborhoods stayed above 4.636 V analytic amplitude; the short boundary dip seen at unity was absent in this fixture. Overall RMS was 3.437, 3.651, and 3.339 V respectively, with maximum adjacent-sample changes 0.217, 0.217, and 0.231 V. EOSG includes secondary voice completions at overlap, so its pulses are not a one-to-one primary-boundary marker in that setting.

The test rack remains unsaved with Morph 0.5 for the user's listening comparison. This supports using the existing Morph control to handle a phase-mismatched recording. It does not prove every arbitrary recording will blend without audible modulation or combing; that remains a content-dependent listening question. No DSP change or new mode was made.

## Temporary panel feedback, 2026-09-24

The development panel now displays all nine frozen Chimera status lights with the shared Leviathan Aperture widgets and text labels: REC, armed, play, pending selection, clock, PM, I/O busy, overload, and error. This makes the REC toggle state visible during live recording. The Rack light descriptions are configured for tooltips/accessibility, and neither light IDs nor audio behavior changed. The SVG remains the temporary unsplit master; the full control/jack layout and status/waveform display remain Phase 8 work. The Rack-linked Chimera test target now links the shared Aperture/NVG implementation used by the widget. Native Linux `make -j4 test-chimera-module plugin.so`: **PASS**. The linked binary has not been installed in the running Rack, so panel rendering still needs a live visual check after reload.

## Phase 5 Clock estimator foundation, 2026-09-24

Added a bounded 48 kHz playback Clock estimator that tracks connection, first accepted phase edge, direct period updates, and waiting timeout. Intervals below two frames are diagnosed without moving the last accepted timestamp; intervals above 2,880,000 frames become a new first edge. Before a period exists the timeout is 48,000 frames after connection or first edge; afterward it is `max(2*period, 48000)`. Disconnect clears the estimator. The Rack module feeds it the existing Schmitt Clock rise, while REC quantization continues to use every raw rise independently. Focused tests check those bounds, reconnection, and the module's first/second Clock edges. Native Linux `make -j4 test-chimera-clock test-chimera-module plugin.so`: **PASS** at this checkpoint. Playback modes followed in the next section.

## Phase 5 Clock playback modes, 2026-09-24

Added persisted `ckop` 0/1/2 and a context-menu selector. A valid Gene Shift edge advances the source origin by one current finite Gene length (or the full Splice length), wraps within the selected region, and forces exactly one onset. It resolves a queued selection or pending `pmod=0` boundary stop before that onset. A coincident natural voice completion is retained for EOSG without adding a second onset. Reverse playback steps backward, and a Clock edge does not start a stopped transport.

Stretch holds its source trajectory before a period is known, then advances by one captured Gene step per Clock interval, independent of pitch magnitude. Edges reanchor future Gene origins without forcing an onset; old readers finish normally. The trajectory freezes after Clock timeout and reverses if base pitch direction reverses. Hybrid `ckop=0` selects Gene Shift below density 1.98 and Stretch above 2.02, retaining its previous side inside that band; explicit modes ignore Morph selection. Mode changes use the engine's existing 48-frame reader transition. Disconnect returns to the free-running Slide origin.

Native Linux `make -j4 test-chimera-clock test-chimera-phase3 test-chimera-phase4 test-chimera-module plugin.so`: **PASS**. Focused fixtures cover forward/reverse/full-Splice steps, source velocity at 0.5× and 2× pitch, timeout hold, selection before forced onset, pending Play stop, hybrid hysteresis, JSON validation, and stopped transport. Grains and Slice ASan/UBSan binaries pass with leak detection disabled; the plain `make test-chimera-phase3-sanitize` command hit the known LeakSanitizer-under-ptrace runtime failure. The full-capacity free-running benchmark after these changes measured 0.85–0.92% of one core, and the Rack-linked module-callback benchmark 1.12–1.19% of one core, over four ten-second equivalent runs each. These omit Rack graph/GUI/SRC overhead and do not yet measure sustained clocked playback. Live Clock listening and the complete Phase 5 event matrix remain open.

## Live Clock comparison and forced-transition correction, 2026-09-24

The user confirmed the new Aperture light array is visible in Rack. In the disposable 48 kHz session, the restarted Chimera had no Reel: Octavia A and D were silent while the VCO source on C was 3.653 V RMS. A fresh direct-input Current/initial-Append recording was started and ended with two distinct REC presses; afterward A was 3.652 V RMS and a three-second A/D capture had EOSG rises spaced 35,082 core frames. The temporary diagnostic Control A cable was patched into Chimera CLOCK, with four 10 ms / 5 V pulses at 0.5, 1.5, 2.5, and 3.5 seconds of each frame-synchronized four-second recording. Both recordings used the same Gene Size 0.27, Morph 1/6, and S.O.S. 1.

Explicit Gene Shift `ckop=1` recording `octavia-1790269946092-2-chimera-clock-gene-shift.wav` showed no EOSG pulse within 100 frames of any forced edge, but three edges had 2.272, 0.911, and 2.938 V adjacent-sample jumps at edge+2 frames; the remaining edge followed the ordinary sine step. Explicit Stretch `ckop=2` recording `octavia-1790270027787-3-chimera-clock-stretch.wav` had maximum steps around the four edges of 0.217 V, consistent with the ordinary sine slope. The Shift jumps exposed a forced-transition mix defect: a new voice in a different slot entered at full weight while the old slot's 48-frame tail was still present.

The engine now applies a common 48-frame incoming ramp to all active slots after a forced transition, while outgoing tails keep their existing fade. A phase-misaligned sine regression with three Clock Shift edges requires adjacent normalized output steps below 0.15 and preserves the one-onset/one-completion edge tests. Native Linux `make -j4 test-chimera-phase4 test-chimera-phase3 test-chimera-module plugin.so`: **PASS**; Grains and Slice ASan/UBSan (leak detection disabled): **PASS**. Four full-capacity core benchmark runs measured 0.885–1.015% of one core after the correction. The fixed build has not yet been loaded in Rack, so the audible result remains unverified. The temporary Control A cable was removed; `ckop=0`, Morph 1, and REC 0 were restored, leaving the unsaved test Reel available until the next restart. No patch was saved.

## Live fixed-build Clock retest, 2026-09-24

The user loaded the corrected Linux build and restarted the disposable 48 kHz Rack. A new direct-input VCO Reel was recorded with two distinct REC presses. Octavia A confirmed 3.660 V RMS wet output. With Morph 1/6, Gene Size 0.27, S.O.S. 1, explicit `ckop=1`, and the same four 10 ms / 5 V Clock pulses at 0.5/1.5/2.5/3.5 seconds, the four-second A/D recording `octavia-1790270353551-1-chimera-clock-gene-shift-fixed.wav` had an ordinary p99 adjacent-sample step of 0.216 V. The largest steps within roughly 200 frames of the four forced edges were 0.216, 0.231, 0.216, and 0.217 V respectively, versus 0.911–2.938 V for three edges in the earlier build. No EOSG rise occurred within 100 frames of a forced edge. This validates the corrected transition for this sine Reel and event pattern; other content and broader event collisions remain to audition. The temporary Control A→CLOCK cable was removed, Octavia presence released, `ckop=0` and Morph 1 restored, and REC remains off. The new unsaved Reel remains in the disposable session; no patch was saved.

The user heard a smaller recurring blip at Morph 0.5 despite the measured absence of a deep envelope dip. At very high Morph, the user described a sequence-like texture. This is consistent with the implemented onset law: density rises to four voices, and above Morph 5/6 the secondary slots probabilistically use the configured 2×/3×/4× ratios with stereo position draws. The report is subjective, not a measured hardware comparison or proof that every blip is a defect. Keep the residual texture in the Phase 4 listening ledger; future decisions should compare varied recorded material at unity and overlap before altering Profile 1 DSP.

## Phase 5 same-frame selection and recorder priority, 2026-09-24

The module callback now resolves the current frame's controls and Organize/Shift selection before REC requests and their coincident Clock edge can start a Current writer. The resulting control frame is consumed once by the Slice audio step, so control smoothing, onset accounting, and callback allocation behavior retain their single-step cadence. A Clock-armed Current start now latches the Splice committed by a same-frame Shift; an Organize change is applied before Shift, including wraparound. The pending `pmod=0` Clock stop also consults the current Morph-derived hybrid Clock mode rather than the prior frame's mode.

Rack-linked tests cover Shift+REC+Clock and Organize+Shift+REC+Clock writer destinations. Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-clock test-chimera-module plugin.so`: **PASS** (the first module run exposed a fixture interaction with the snapshot overwrite check; restoring its first-region selection made the rerun pass). This is a focused part of the section 13.2 event order. Options transactions, button chords, Reel Mode, rsop, gain switching, and the broader collision matrix remain open. The new binary has not been loaded into Rack.

## Phase 5 REC assignment and input gain, 2026-09-24

Added persisted `rsop=0/1`: the default REC button/gate start chooses Current/Append respectively, while the alternate semantic command chooses the other destination. Existing active or armed recording toggles still stop or cancel according to their state; an explicit Start Append command remains Append. The context menu exposes the assignment and alternate command. Physical REC+SPLICE chord detection is not implemented yet.

Added persisted input gain with −3, 0, +6, and +12 dB targets, default 0 dB. Gain is applied to the normalized live stereo stream before input DC filtering and S.O.S./record routing, with a 240-core-frame linear transition. An interrupted change starts from the current multiplier. The PM source and left-input presence detector still use the raw pre-gain input. The context menu exposes all four choices. Slice tests check first/last fade frames and an interruption; Rack-linked tests check `rsop` default/alternate behavior and valid/invalid JSON reload. Native Linux `make -j4 test-chimera-phase3 test-chimera-module plugin.so`: **PASS**. The new binary has not been loaded into Rack. Button chords, Reel Mode, option-text transactions, and broader Phase 5 event collisions remain open.

## Phase 5 button release and chord arbitration, 2026-09-24

Added a bounded, allocation-free button gesture resolver. REC, SHIFT, and SPLICE panel actions now occur on release. REC+SPLICE emits the alternate recording command on the first release; SHIFT+REC cycles the four input-gain settings; subsequent releases are suppressed until all buttons are up. Involvement of all three buttons cancels the gesture. REC/SPLICE/SHIFT gate jack rises remain independent, sample-timed events and never join a button chord. The unsupported-host-rate guard cancels held button gestures so a release after returning to 48 kHz cannot replay an old action. Existing Rack tests that intend a semantic REC event now use the explicit command path; dedicated button tests cover release timing, chords, suppression, three-button cancellation, and gate independence.

SHIFT+SPLICE is recognized and suppresses its constituent single actions, but Reel Mode has no bank switch implementation yet, so it has no Reel effect. This chord remains open with the Reel Mode work. Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-clock test-chimera-module plugin.so`: **PASS**. The linked build has not been loaded into Rack. Options-text transactions and the broader same-frame matrix also remain open.

## MVP scope correction: one Reel per module, 2026-09-24

The user chose one Reel per Chimera instance for the MVP. Multiple Reels are achieved by patching multiple module instances. The 32-slot bank, Reel Mode, and bank UI/import/export obligations have been removed from the MVP specification, plan, and acceptance cases; the original hardware brief remains unchanged as source material. Organize continues to choose Splices inside the one Reel. A future action that genuinely requires multiple buttons must use a visible latching UI so a mouse user can operate it sequentially.

The recent panel-button chord resolver was removed because normal Rack mouse interaction cannot reliably use it. REC, SHIFT, and SPLICE each resolve on their own release; alternate recording and input gain remain explicit context-menu actions. Gate jack rises stay independent. Rack-linked `test-chimera-module` and full native Linux `plugin.so` link: **PASS**. The older chord entry above records a superseded build and is not the current behavior. No new live Rack session was needed or loaded for this scope correction.

The user clarified that local WAV load/export is the intended way to move Reels between uses. The specification now has a mouse-complete UI contract in section 14.1.1, and GUI-004 requires a single-pointer workflow through Record Current/Append, Splices, gain/options, one-Reel WAV load/export, and Rack patch save/reload. This is a target for Phase 7 file I/O and Phase 8 production UI. The current temporary panel still lacks visible SHIFT, SPLICE, and Organize controls, and the current build still cannot load/export WAV or embed recorded audio in a patch; these are not claimed complete by this document update.

## Phase 5 Vari-Speed option, 2026-09-24

Wired the existing Core `vsop` rate maps into the Chimera audio callback and exposed persisted `vsop=0/1/2` in a checked Vari-Speed mode menu: classic CV, bidirectional 1 V/oct, and forward-only 1 V/oct. Invalid saved enum values leave the prior setting intact. Rack-linked tests drive actual source positions under ±2× pitch mode, forward-only +2×, and forward-only Stop; JSON roundtrip and invalid-value retention are also covered. The reverse test uses frame 997 after one −2× step because a reverse origin begins at frame 999 of a 1,000-frame region. Native Linux `make -j4 test-chimera-phase1 test-chimera-phase3 test-chimera-module plugin.so`: **PASS**. The new binary has not been loaded into Rack. The options-text importer/exporter and broader Phase 5 same-frame matrix remain open.

## Phase 5 due-completion event collision, 2026-09-24

Added isolated Slice and Rack-linked fixtures where a finite natural completion shares a frame with Shift, a PLAY retrigger, a Current REC request, and Gene Shift Clock. The selected writer destination is latched before the Clock start, one new onset occurs, and the real prior completion still raises EOSG. The fixture passes. A second `pmod=0` boundary-stop fixture found that stopping at a due completion reset Grains before its pending natural completion could be emitted. The stop path now drains that already-due event before reset, emits its EOSG pulse on the stop frame, and clears EOSG on later stopped frames. Forced stops before expiry still create no completion. Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-module plugin.so`: **PASS**, including the full `plugin.so` link. The Slice ASan/UBSan run also passes with `ASAN_OPTIONS=detect_leaks=0`. This extends TRN-020 coverage; the complete same-frame matrix and options-text transactions remain open. The build has not been loaded into Rack.

## Phase 5 options-text transactions, 2026-09-24

Added context-menu import and export for the twelve firmware-style option keys. The text parser accepts whitespace-separated values, blank lines, and `#` comments; it validates the complete bounded file before applying any recognized setting. Invalid enums, ratios, duplicate keys, extra fields, and unsafe or oversized tokens reject the edit. Unknown safe keys produce warnings and remain in an inert extras map, which is preserved in patch JSON and re-exported. An accepted edit is staged on the control side and adopted together at the start of one 48 kHz core frame without allocation or a mutex in the audio callback. A second import waits for that adoption; patch save sees the complete staged transaction.

Rack-linked tests cover parse/export roundtrip, malformed-file rollback, staged save, one-frame allocation-free adoption, and extras persistence through patch reload. Native Linux `make -j4 test-chimera-module plugin.so`: **PASS**, including the full plugin link. The new binary has not been loaded into Rack; a live integration check is optional for this text-file/UI path. Phase 5 still needs the complete event and option acceptance matrix, including PM presence/routing, before its exit gate can close.

## Phase 5 PM acceptance expansion, 2026-09-24

Added focused OUT-006..008 fixtures around the existing PM implementation. A quiet connected left input retains right live audio through the three-second dwell; PM entry removes it over 240 core frames, and left-signal return restores it over the same interval without copying left into right. A below-threshold raw left signal still enters PM with +12 dB input gain, confirming that the detector reads pre-gain input. A linear Reel checks signed 96-frame-per-volt displacement at ±1 V and ±20 V, including the raw ±10 V clamp and unchanged primary cursor. A shifted-source comparison under high-Morph moving playback checks that the same PM displacement reaches all overlapping readers. The earlier Current-recording and marker fixtures continue to cover isolation of PM CV from the writer and marker address.

Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-clock test-chimera-module plugin.so`: **PASS**. The plugin link was already current from the options-text build. No DSP source change or live Rack session was needed for these coverage additions. Phase 5 remains open for the remaining transport/recording collision cases and complete option behavior matrix.

A Rack-linked TRN-021 fixture now queues a Shift selection before the natural Gene expiry, then sends a Gene Shift Clock edge. It verifies one committed region change and one forced onset, with no fabricated EOSG completion. The existing pending PLAY-stop Clock fixture covers the other TRN-021 branch. `make -j4 test-chimera-module`: **PASS**.

## Phase 5 clocked REC and simultaneous gate acceptance, 2026-09-24

Added Rack-linked REC-007/008 and TRN-019 cases for two REC commands canceling ArmedStart or ArmedStop, a Clock edge after each cancellation, a stop command coincident with the Clock edge excluding that frame, and Clock disconnect canceling either arm while leaving an already active Current recording running. A REC and SPLICE gate rise on the same Append-start frame records that frame; its duplicate start marker is deferred and coalesced when Append finalizes. A separate same-frame REC-stop/SPLICE-rise fixture confirms the independent SPLICE gate creates a playback marker while recording stops.

Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-clock test-chimera-module plugin.so`: **PASS**; the existing full plugin link was current. These cases needed no DSP change or live Rack. Phase 5 remains open for the remaining acceptance matrix and a focused final integration check.

## Phase 5 mouse-complete temporary menu and immediate-option check, 2026-09-24

The temporary panel still omits the physical SHIFT/SPLICE controls and Organize knob. Added explicit context-menu actions for Record Current, Record Append, Next Splice, and Add Marker; relabeled the alternate REC and Stop/cancel actions to expose their behavior. Next Splice and Add Marker use a small atomic command bitset, consumed in their specified core-frame priority positions and cleared by the unsupported-rate guard. Rack-linked tests verify explicit Current/Append remain independent of `rsop`, menu Shift queues one selection, menu marker capture preserves the stable pending Splice ID, and commands from a 96 kHz guard interval do not replay after returning to 48 kHz.

Added a Rack-linked `omod` fixture: a mid-cycle Splice request remains pending in default mode, then enabling immediate transitions commits it on the next core frame with one new onset and no old-region residual. Native Linux `make -j4 test-chimera-phase3 test-chimera-phase4 test-chimera-clock test-chimera-module plugin.so`: **PASS**, including the full plugin link from the menu-command change. A live Rack check is not needed for this source path; production panel controls and visible status text remain Phase 8 UI work.

## Phase 5 complete control access on the development panel, 2026-09-24

Replaced Chimera's sparse temporary panel layout with a 28 HP anchored layout in the editable `res/Chimera.svg` master. The Rack widget now presents all six main knobs, three CV attenuverters, REC/SPLICE/SHIFT buttons, six control-CV inputs, five gate inputs, both audio inputs and outputs, CV OUT, EOSG, and the existing Aperture LED array. The widget resolves positions through `PanelSvgUtils`; the generated anchor atlas was refreshed. The SVG splitter generated `res/Chimera.panel.svg` and outlined `res/Chimera.labels.svg` runtime assets from the master. The labels use a Rack framebuffer cache. The established parameter and port enum ordering was preserved.

An Inkscape render and component-size fit overlay were inspected at 1200 px; labels were moved clear of knob footprints and divider lines crossing jack labels were removed. A check found a unique anchor for all 38 controls, ports, and lights. Native Linux `make -j4 test-chimera-module plugin.so`: **PASS**, including full linking. This is a source/render fit check, not an in-Rack visual or interaction audition. Detailed transport/recording status text, waveform display, final artwork, and WAV/patch audio persistence remain outside this Phase 5 panel pass.
